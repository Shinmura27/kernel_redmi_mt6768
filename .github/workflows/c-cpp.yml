name: Kernel Upstreamer

on:
  push:
    branches: [ base-r-oss ]
  pull_request:
    branches: [ base-r-oss ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
